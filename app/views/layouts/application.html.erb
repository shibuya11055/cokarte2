<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "cokarte" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%# Favicon / PWA icons（画像未配置でもエラーにしない） %>
    <% begin %>
      <%= favicon_link_tag 'cokarte-favicon.png' %>
    <% rescue Propshaft::MissingAssetError %>
    <% end %>
    <% begin %>
      <link rel="apple-touch-icon" href="<%= asset_path 'cokarte-apple-touch-icon.png' %>">
    <% rescue Propshaft::MissingAssetError %>
    <% end %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# Webフォント（Noto Sans JP）でデバイス間のフォント差異を解消 %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;500;700&display=swap" rel="stylesheet">
    <%# Select2 and jQuery for searchable selects %>
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" data-turbo-track="reload" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" data-turbo-track="reload"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js" data-turbo-track="reload"></script>
  </head>

  <body<% if (controller_name == 'pages' && action_name == 'home') || !user_signed_in? %> class="no-side-menu"<% end %><% if user_signed_in? && !(controller_name == 'pages' && action_name == 'home') %> data-controller="sidebar"<% end %>>
    <% if user_signed_in? && !(controller_name == 'pages' && action_name == 'home') %>
      <div class="app-topbar">
        <button type="button" class="hamburger" aria-label="メニュー" aria-controls="app-side-menu" aria-expanded="false"
                data-sidebar-target="toggle" data-action="click->sidebar#toggle">
          <span></span>
          <span></span>
          <span></span>
        </button>
        <div class="topbar-title">cokarte</div>
      </div>
      <div class="side-overlay" data-sidebar-target="overlay" data-action="click->sidebar#close" aria-hidden="true"></div>
      <%= render 'layouts/side_menu' %>
    <% end %>
    <main class="main-content">
      <% flash.each do |k, v| %>
        <div class="form-errors"><%= v %></div>
      <% end %>
      <%= yield %>
    </main>
    <% if controller_name == 'pages' && action_name == 'home' %>
      <footer class="site-footer" style="border-top:1px solid #eee; margin-top:24px;">
        <div class="footer-container" style="max-width:980px; margin:0 auto; display:flex; gap:16px; align-items:center; justify-content:space-between; padding:12px 16px; font-size:0.95em;">
          <div class="links">
            <%= link_to 'ホーム', root_path %>
            <%= link_to '特定商取引法に基づく表記', legal_path %>
            <%= link_to '利用規約', terms_path %>
            <%= link_to 'プライバシー', privacy_path %>
          </div>
          <div class="auth">
            <% unless user_signed_in? %>
              <%= link_to 'ログイン', new_user_session_path %>
              <%= link_to '新規登録', new_user_registration_path, style: 'padding:6px 10px; border:1px solid #218a5a; border-radius:8px; margin-left:8px;' %>
            <% end %>
          </div>
        </div>
      </footer>
    <% end %>
  </body>
</html>
